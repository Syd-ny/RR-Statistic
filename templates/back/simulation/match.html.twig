{% extends 'base.html.twig' %}

{% block body %}
    <h1>Sélection des joueurs</h1>

    {{ form_start(form) }}
    {{ form_row(form.player) }}
    {{ form_row(form.player) }}
    <button type="submit" class="btn btn-primary">Simuler le match</button>
    {{ form_end(form) }}
{% endblock %}