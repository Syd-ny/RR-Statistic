{% extends 'base.html.twig' %}

{% block body %}

<!-- la liste des stats des joueurs-->
<div class="col-12 col-lg-10">
    <h2 class="text-muted fw-bold mb-4">Ratings Double</h2>
        {% include 'partials/header/ratings.html.twig' %}
            {% for player in players %}               
                {% include 'partials/player/infos.html.twig' %}
                    {% include 'partials/array/format.html.twig' %}
                        {% include 'partials/player/show.html.twig' %}
                            {% set ratingDouble =  player.ability + player.serve / 2 + player.strenght / 5 + player.speed / 10 + player.mentality / 10 + player.doubles / 2.5 %}
                            <p>{{ ratingDouble + player.hard /100 }} </p>
                        </a>
                    </div>
                    {% include 'partials/array/format.html.twig' %}
                        {% include 'partials/player/show.html.twig' %}
                            {% set ratingDouble =  player.ability + player.serve / 2 + player.strenght / 5 + player.speed / 10 + player.mentality / 10 + player.doubles / 2.5 %}
                            <p>{{ ratingDouble + player.clay /100}}</p>
                        </a>
                    </div>
                    {% include 'partials/array/format.html.twig' %}
                        {% include 'partials/player/show.html.twig' %}
                            {% set ratingDouble =  player.ability + player.serve / 2 + player.strenght / 5 + player.speed / 10 + player.mentality / 10 + player.doubles / 2.5 %}
                            <p>{{ ratingDouble + player.grass /100}}</p>
                        </a>
                    </div>
                    {% include 'partials/array/format.html.twig' %}
                        {% include 'partials/player/show.html.twig' %}
                            {% set ratingDouble =  player.ability + player.serve / 2 + player.strenght / 5 + player.speed / 10 + player.mentality / 10 + player.doubles / 2.5 %}
                            <p>{{ ratingDouble + player.indoor /100}}</p>
                        </a>
                    </div>
                    {% include 'partials/array/format.html.twig' %}
                        {% include 'partials/player/show.html.twig' %}
                            <p>{{ ratingDouble }} </p>
                        </a>
                    </div>
                    <hr class = "my-1">
                </div>
            {% endfor %}
            </div>
                
        </div>

        <!-- pagination -->
        <nav class="col-lg-9 offset-lg-3">
            <ul class="pagination">
                <li class="page-item disabled">
                    <a class="page-link">&laquo;</a>
                </li>
                <li class="page-item active"><a class="page-link" href="#">1</a></li>
                <li class="page-item" aria-current="page">
                    <a class="page-link" href="#">2</a>
                </li>
                <li class="page-item"><a class="page-link" href="#">3</a></li>
                <li class="page-item">
                    <a class="page-link" href="#">&raquo;</a>
                </li>
            </ul>
        </nav>
    </div>
</div>
{% endblock %}




